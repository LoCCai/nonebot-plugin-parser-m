<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ result.title }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'HYSongYunLangHeiW-1', sans-serif;
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
        }

        .container {
            width: 100%;
            max-width: 800px;
            margin: 20px auto;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .header {
            display: flex;
            align-items: center;
            padding: 16px 20px;
            background-color: #ff6b6b;
            color: white;
        }

        .platform-icon {
            width: 32px;
            height: 32px;
            margin-right: 12px;
        }

        .platform-name {
            font-size: 18px;
            font-weight: bold;
        }

        .content {
            padding: 20px;
        }

        .title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 12px;
            color: #333;
        }

        .author {
            display: flex;
            align-items: center;
            margin-bottom: 16px;
        }

        .author-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 12px;
        }

        .author-info {
            display: flex;
            flex-direction: column;
        }

        .author-name {
            font-size: 14px;
            font-weight: bold;
            color: #333;
        }

        .publish-time {
            font-size: 12px;
            color: #999;
        }

        .text {
            font-size: 16px;
            margin-bottom: 20px;
            color: #666;
            line-height: 1.8;
            white-space: pre-wrap;
            word-wrap: break-word;
        }

        .images {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 20px;
        }

        .image {
            flex: 1;
            min-width: calc(33.333% - 8px);
            max-width: 100%;
            border-radius: 8px;
            overflow: hidden;
        }

        .image img {
            width: 100%;
            height: auto;
            display: block;
        }

        .media-info {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
            padding: 12px;
            background-color: #f9f9f9;
            border-radius: 8px;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 14px;
            color: #666;
        }

        .info-item .icon {
            width: 16px;
            height: 16px;
        }

        .footer {
            padding: 16px 20px;
            background-color: #f9f9f9;
            border-top: 1px solid #eee;
        }

        .url {
            font-size: 14px;
            color: #999;
            word-break: break-all;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background-color: #ff6b6b;
            color: white;
            text-decoration: none;
            border-radius: 20px;
            font-size: 14px;
            font-weight: bold;
            margin-top: 12px;
        }

        .btn:hover {
            background-color: #ff5252;
        }

        .btn .icon {
            width: 16px;
            height: 16px;
        }

        .video-preview {
            position: relative;
            width: 100%;
            aspect-ratio: 16 / 9;
            margin-bottom: 20px;
            border-radius: 8px;
            overflow: hidden;
        }

        .video-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .play-btn {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 64px;
            height: 64px;
            background-color: rgba(0, 0, 0, 0.6);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .play-btn::after {
            content: '';
            width: 0;
            height: 0;
            border-style: solid;
            border-width: 12px 0 12px 20px;
            border-color: transparent transparent transparent white;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img class="platform-icon" src="{{ platform_icon }}" alt="TapTap">
            <div class="platform-name">TapTap</div>
        </div>

        <div class="content">
            <h1 class="title">{{ result.title }}</h1>

            {% if result.author %}
            <div class="author">
                {% if result.author.avatar %}
                <img class="author-avatar" src="{{ result.author.avatar }}" alt="{{ result.author.name }}">
                {% endif %}
                <div class="author-info">
                    <div class="author-name">{{ result.author.name }}</div>
                    {% if result.timestamp %}
                    <div class="publish-time">{{ result.formartted_datetime }}</div>
                    {% endif %}
                </div>
            </div>
            {% endif %}

            {% if result.text %}
            <div class="text">{{ result.text }}</div>
            {% endif %}

            {% if result.extra.images %}
            <div class="images">
                {% for image in result.extra.images %}
                <div class="image">
                    <img src="{{ image }}" alt="">
                </div>
                {% endfor %}
            </div>
            {% endif %}

            {% if result.extra.stats %}
            <div class="media-info">
                {% if result.extra.stats.likes %}
                <div class="info-item">
                    <span class="icon">❤️</span>
                    <span>{{ result.extra.stats.likes }}</span>
                </div>
                {% endif %}
                {% if result.extra.stats.comments %}
                <div class="info-item">
                    <span class="icon">💬</span>
                    <span>{{ result.extra.stats.comments }}</span>
                </div>
                {% endif %}
                {% if result.extra.stats.shares %}
                <div class="info-item">
                    <span class="icon">🔗</span>
                    <span>{{ result.extra.stats.shares }}</span>
                </div>
                {% endif %}
            </div>
            {% endif %}
        </div>

        <div class="footer">
            <div class="url">{{ result.url }}</div>
            <a class="btn" href="{{ result.url }}" target="_blank">
                <span class="icon">🔗</span>
                <span>查看原文</span>
            </a>
        </div>
    </div>
</body>
</html>